console.log("Connected");
function closerNumber(theNumber, secondNumber){
    if(theNumber || secondNumber <= 0){S
        alert(`Sorry. ${theNumber} is a negative number. Try again.`)
    }
    

}

closerNumber(-3, 5);

/*console.log("--------------------")  ;

let counts = [4, 9, 15, 6, 2],
  goal = prompt();

let closest = counts.reduce(function(prev, curr) {
  return (Math.abs(curr - goal) < Math.abs(prev - goal) ? curr : prev);
});

console.log(closest);*/