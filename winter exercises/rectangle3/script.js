
function rectangleCalculator(length, width){
    parseFloat(length, width);
    let result = 2 * (length + width);
    console.log(`The perimeter of your rectangle is: ${result}`);
    return;
}

rectangleCalculator(6, 2);
